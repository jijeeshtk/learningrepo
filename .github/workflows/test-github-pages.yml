name: 🧪 Test Hugo Build

on:
  workflow_dispatch:
  pull_request:

jobs:
  test:
    uses: jijeeshhomelab/learningrepo/.github/workflows/test-build-pages.yml@main
    secrets: inherit   # <-- no need for GH_ACTIONS_TOKEN here
