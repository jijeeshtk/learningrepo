name: 🧪 Test Hugo Build

on:
  workflow_dispatch:
  pull_request:

jobs:
  test:
    uses: jijeeshtk/Mylearning/.github/workflows/test-build-pages.yml@main
    secrets:
      GH_ACTIONS_TOKEN: ${{ secrets.GH_ACTIONS_TOKEN }}
