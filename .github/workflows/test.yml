# .github/workflows/cron-smoke.yml
name: Cron Smoke
on:
  schedule:
    - cron: "0 */1 * * *"  # every 15 min UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          date -u
          echo "event=${{ github.event_name }}"
